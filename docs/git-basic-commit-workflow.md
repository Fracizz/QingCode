# Git 本地基础提交流程

> 状态：**方案已确认，待实现**。
> 决策：支持暂存、取消暂存和本地提交；提交时只提交已暂存内容。

## 1. 背景

QingCode 当前的 Git 集成以只读查看为主：

- 源代码管理面板展示工作区变更
- 资源管理器、编辑器标签和状态栏展示 Git 状态
- 支持查看 HEAD 与工作区之间的差异
- 不支持暂存、取消暂存或提交

本次增强补齐最小可用的本地提交闭环，同时保持功能简单，不引入远程凭据、冲突处理和高风险恢复操作。

## 2. 产品目标

用户可以在源代码管理面板中完成：

1. 区分已暂存与未暂存的更改
2. 暂存单个文件或全部文件
3. 取消暂存单个文件或全部文件
4. 输入提交信息
5. 仅提交已暂存内容
6. 在操作完成后立即看到最新 Git 状态

## 3. 范围

### 3.1 本次实现

| 功能 | 要求 |
|---|---|
| 状态分组 | 显示“已暂存的更改”和“更改”两组 |
| 单文件暂存 | 文件行按钮和右键菜单均可执行 |
| 全部暂存 | “更改”分组提供全部暂存操作 |
| 单文件取消暂存 | 文件行按钮和右键菜单均可执行 |
| 全部取消暂存 | “已暂存的更改”分组提供全部取消暂存操作 |
| 提交信息 | 源代码管理面板顶部提供多行输入框 |
| 本地提交 | 只提交暂存区，不自动暂存未暂存文件 |
| 状态刷新 | 每次 Git 写操作后执行完整状态刷新 |
| 错误反馈 | 展示 Git 原始错误的可读摘要，并保留用户输入 |

### 3.2 明确不做

- push、pull、fetch 和远程凭据管理
- 分支创建、切换、合并和 rebase
- stash
- discard / restore 工作区内容
- amend、签名提交和交互式暂存
- 冲突解决器

这些功能涉及认证、冲突、历史修改或数据丢失风险，不属于本次简单增强。

## 4. 交互方案

源代码管理面板从上到下排列：

```text
源代码管理                         刷新
当前分支

[ 提交信息输入框                 ]
[ 提交（仅已暂存）               ]

已暂存的更改  2                 [-]
  M  src/a.ts
  A  src/b.ts

更改          3                 [+]
  M  src/a.ts
  D  src/c.ts
  U  src/new.ts
```

### 4.1 提交规则

- 提交信息去除首尾空白后不能为空。
- 暂存区为空时不可提交，并提示“请先暂存要提交的更改”。
- 点击提交不会自动暂存任何文件。
- 提交成功后清空提交信息并刷新状态。
- 提交失败时保留提交信息与当前列表，展示错误原因。
- Git hooks 正常执行；hook 失败视为提交失败，并显示 hook 输出。

### 4.2 文件操作

- “更改”组文件行提供暂存按钮。
- “已暂存的更改”组文件行提供取消暂存按钮。
- 同一操作同步加入文件右键菜单，保证按钮不明显时仍可发现。
- 分组标题提供全部暂存或全部取消暂存。
- 操作进行时禁用重复点击，并显示轻量加载状态。
- 文件点击仍沿用现有差异查看行为。

### 4.3 双状态文件

Git 的 porcelain 状态包含索引区和工作区两列 `XY`。例如：

- `M `：文件已暂存，工作区没有后续修改
- ` M`：只有未暂存修改
- `MM`：已暂存后又继续修改
- `AM`：新增文件已暂存后又继续修改
- `??`：未跟踪文件

`MM`、`AM` 等文件必须同时出现在两组中：

- 已暂存组代表 `HEAD → index`
- 更改组代表 `index → worktree`

对其中一组执行操作后，另一组仍按 Git 实际状态保留。

## 5. 状态模型调整

当前 `git.rs` 的 `parse_status` 会对 `XY` 调用 `trim()`，`git_status.rs` 也会把部分双列状态压缩成单字符。这会导致 `M ` 与 ` M` 都变成 `M`，无法判断更改位于暂存区还是工作区。

实现前必须统一状态契约：

- Rust 返回完整的两字符 `XY`，特殊状态保留 `??` 和 `!!`
- 前端以 `indexStatus` 和 `worktreeStatus` 辅助函数解释 `XY`
- 现有树、标签和角标继续通过统一 glyph 函数显示单个用户友好字母
- SCM 分组只依据完整 `XY`，不依据压缩后的展示字符
- 轻量状态接口和完整状态接口必须返回一致的状态语义

建议继续保留 `GitChange.status: string`，避免扩大序列化结构；但该字段的新契约必须明确为未经裁剪的 porcelain `XY`。

## 6. 后端设计

核心文件：

- `src-tauri/src/git.rs`
- `src-tauri/src/git_status.rs`
- `src-tauri/src/lib.rs`
- `src-tauri/src/path_guard.rs`

### 6.1 新增 Tauri 命令

#### `git_stage`

参数：

```text
path: 项目根目录
files: 项目内相对路径数组
```

行为：

- 校验项目根目录位于 `PathAllowlist`
- 校验每个文件路径不为空、无父目录穿越且位于项目内
- 执行 `git add -- <files...>`
- 全部暂存由前端传入当前未暂存文件列表，不使用隐式全仓库 shell 命令

#### `git_unstage`

参数与 `git_stage` 相同。

行为：

- 优先执行 `git restore --staged -- <files...>`
- 对尚无 HEAD 的首次提交仓库使用兼容回退
- 不修改工作区文件内容

#### `git_commit`

参数：

```text
path: 项目根目录
message: 提交信息
```

行为：

- 后端再次校验提交信息非空
- 通过进程 stdin 向 `git commit --file=-` 传递提交信息
- 不拼接 shell 字符串，避免命令注入和转义问题
- 不添加 `-a`，确保只提交暂存区
- 保留 Git hooks、用户身份检查和仓库配置行为
- 返回提交成功摘要；失败时返回 stderr/stdout 中可读的错误信息

### 6.2 命令执行约束

- 继续使用系统 Git CLI，不引入 libgit2
- Windows 保持 `CREATE_NO_WINDOW`
- 所有文件参数必须位于 `--` 之后
- Git 写操作放入 `spawn_blocking`，避免阻塞 Tauri 异步运行时
- 不通过 shell 执行命令
- 不静默吞掉写操作错误

### 6.3 命令注册

在 `src-tauri/src/lib.rs` 的 `invoke_handler` 中注册：

- `git::git_stage`
- `git::git_unstage`
- `git::git_commit`

## 7. 前端设计

核心文件：

- `src/components/SourceControlPanel.tsx`
- `src/lib/git.ts`
- `src/lib/gitStatus.ts`
- `src/store/gitStatusStore.ts`
- `src/store/sourceControlStore.ts`

### 7.1 状态辅助函数

在 `src/lib/gitStatus.ts` 增加纯函数：

- 读取 index 状态
- 读取 worktree 状态
- 判断文件是否存在已暂存更改
- 判断文件是否存在未暂存更改
- 将变更拆分为 staged / unstaged 两组
- 为每组计算正确的 glyph 和颜色

这些函数必须覆盖 `M `、` M`、`MM`、`AM`、`AD`、`R `、`??` 等状态。

### 7.2 面板状态

`SourceControlPanel` 增加：

- `commitMessage`
- 当前 Git 操作类型
- 当前正在处理的文件
- 提交、暂存和取消暂存处理函数

Git 写操作统一走：

```text
校验输入
  → safeInvoke
  → 完整 git_status 刷新
  → 同步 gitStatusStore/sourceControlStore
  → 成功或失败反馈
```

写操作后必须使用完整 `git_status`，避免轻量缓存短暂丢失 staged / unstaged 分组信息。

### 7.3 列表结构

现有单个虚拟列表改为两个可独立为空的分组：

- 已暂存的更改
- 更改

大量文件时仍需保留虚拟化，避免一次性渲染所有文件。可以为每组各使用一个列表，或将分组标题和文件行扁平化为一个虚拟列表；实现时优先选择状态简单、滚动体验稳定的方式。

### 7.4 反馈方式

- 成功：使用现有 toast，例如“已暂存 3 个文件”“提交成功”
- 输入或状态问题：在提交区就地提示
- Git 命令失败：使用 toast，并在面板错误区域保留可读信息
- 不使用浏览器原生 `alert`、`prompt` 或 `title`
- 悬停说明继续使用共享 `Tooltip`

## 8. 安全与异常处理

| 场景 | 预期行为 |
|---|---|
| 项目不是 Git 仓库 | 不展示提交操作，沿用当前空态 |
| 系统未安装 Git | 展示“无法运行 Git”错误，不清空输入 |
| Git 用户名或邮箱未配置 | 展示 Git 的身份配置错误 |
| pre-commit hook 失败 | 提交失败，显示 hook 输出并保留消息 |
| 暂存文件随后被删除 | 以 Git 命令结果为准，失败后刷新状态 |
| 文件名包含空格或中文 | 通过参数数组和 `--` 正常处理 |
| 文件名以 `-` 开头 | `--` 阻止其被解释为 Git 参数 |
| 路径越过项目根目录 | 后端拒绝操作 |
| 仓库尚无首次提交 | 支持暂存、取消暂存和首次提交 |
| 提交期间再次点击 | 按钮禁用，不启动并发提交 |

## 9. 测试计划

### 9.1 Rust

在 Git 模块附近增加测试：

1. porcelain `XY` 完整保留
2. staged、unstaged 和双状态解析
3. 暂存普通修改文件
4. 暂存未跟踪文件
5. 暂存删除文件
6. 取消暂存但不恢复工作区内容
7. 无 HEAD 仓库取消暂存
8. 仅提交已暂存版本
9. 未暂存内容在提交后仍保留
10. 空提交信息被拒绝
11. 项目外路径被拒绝
12. 中文、空格及短横线开头文件名

真实 Git 测试使用临时仓库，并仅在仓库内配置测试用 `user.name` 和 `user.email`。

### 9.2 前端

在 `src/lib/gitStatus.test.ts` 及相关 store 测试中覆盖：

- staged / unstaged 分组
- `MM` 和 `AM` 同时进入两组
- `??` 只进入未暂存组
- 展示 glyph 不受完整 `XY` 契约影响
- 完整状态刷新正确覆盖缓存
- 提交按钮对空消息、空暂存区和进行中状态正确禁用

### 9.3 手动验收

使用 `pnpm tauri:dev` 验证：

1. 修改已跟踪文件并暂存
2. 暂存后继续修改同一文件，确认同时出现在两组
3. 提交后确认仅暂存版本进入 commit，后续修改仍保留
4. 暂存并提交未跟踪文件
5. 暂存删除文件
6. 全部暂存和全部取消暂存
7. 首次提交仓库
8. 未配置 Git 身份时的错误反馈
9. hook 拒绝提交时的错误反馈
10. 中文、空格文件名

自动检查：

```bash
pnpm check
pnpm tauri build --no-bundle
```

## 10. 验收标准

- 用户无需离开 QingCode 即可完成一次本地提交
- 提交绝不会隐式包含未暂存文件
- `MM`、`AM` 等双状态文件展示和操作正确
- 取消暂存不会丢失工作区内容
- 所有写操作均受项目路径白名单保护
- Git hooks 和身份配置错误可见且可理解
- 写操作完成后面板、资源管理器、标签和状态栏状态一致
- 现有 Git diff、文件打开、右键菜单和自动刷新功能无回归

## 11. 建议实施顺序

1. 统一两个 Rust 状态接口的完整 `XY` 契约
2. 增加前端状态解释函数及单元测试
3. 实现并测试 stage / unstage Rust 命令
4. 完成 SCM 两组列表和文件操作
5. 实现 commit Rust 命令
6. 完成提交输入区、错误反馈和刷新链路
7. 执行完整自动检查与 Tauri 手动验收

